- `POC_PROCEDURE.md` — langkah lengkap POC LLM serving di Mac Mini (install Ollama, pull model kandidat, benchmark, uji kualitas). **Mulai dari sini.**
- `benchmark_model.py` — skrip ukur token/detik untuk satu model, dipakai di langkah 3 `POC_PROCEDURE.md`.
- `start_ollama.sh`, `healthcheck.sh` — belum dibuat, menyusul di Fase 6 (Security Hardening / kill switch, ROADMAP.md §10) setelah model produksi terpilih.
